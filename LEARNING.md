# RLE (Run-Length Encoding) (lossless data compression)

- consecutive occurrences of the same data value (called a "**run**") 
- space complexity of O(n) - n is the size of the input data
- Ex: AAAAABBBCC -> 5A3B2C